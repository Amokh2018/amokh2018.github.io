import {useState} from 'react';
import Icon from './Icon';
const nodes=[{name:'Specifications',x:85,y:90,description:'Technical documents and 3GPP specifications form the source knowledge.'},{name:'Knowledge graph',x:280,y:65,description:'Entities and relationships connect concepts across the source material.'},{name:'Retrieval',x:170,y:215,description:'Semantic and graph retrieval bring relevant context to a question.'},{name:'Agent workflow',x:385,y:205,description:'Tools and orchestration turn retrieved context into a task-specific workflow.'},{name:'Evaluation',x:280,y:340,description:'Evaluation examines evidence, answer quality and the limits of the system.'}];
export default function Graph(){const [selected,setSelected]=useState(1);return <div className="knowledge-visual"><div className="visual-label"><span>3GPP KNOWLEDGE FABRIC</span><span className="tiny-label">CONCEPT MAP</span></div><div className="graph-stage"><svg viewBox="0 0 470 400" aria-hidden="true"><defs><linearGradient id="edge"><stop stopColor="#e7b96d"/><stop offset="1" stopColor="#70afb9"/></linearGradient></defs>{[[0,1],[0,2],[1,2],[1,3],[2,3],[2,4],[3,4]].map(([a,b])=><line key={`${a}${b}`} x1={nodes[a].x} y1={nodes[a].y} x2={nodes[b].x} y2={nodes[b].y} stroke="url(#edge)" strokeWidth="1"/>)}{nodes.map((n,i)=><circle key={n.name} cx={n.x} cy={n.y} r={i===selected?32:23} className={i===selected?'selected-node':'node'}/>)}</svg>{nodes.map((n,i)=><button key={n.name} style={{left:`${n.x/470*100}%`,top:`${n.y/400*100}%`}} className={`graph-node ${i===selected?'selected':''}`} aria-pressed={selected===i} onClick={()=>setSelected(i)}><Icon name={['book','network','focus','workflow','shield'][i]} size={20}/><span>{n.name}</span></button>)}</div><p className="graph-description" aria-live="polite">{nodes[selected].description}</p></div>}
